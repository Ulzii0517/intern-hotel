import { NavigationWhite } from "../components";
import { HeaderProfile } from "../components/HeaderProfile";

export const ProfilePage = () => {
  return (
    <main>
      <NavigationWhite />;
      <HeaderProfile />
    </main>
  );
};
